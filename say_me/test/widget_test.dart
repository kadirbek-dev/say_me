import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App initializes without crashing', (WidgetTester tester) async {
    // Базовый тест-заглушка для сборки
    expect(true, isTrue);
  });
}